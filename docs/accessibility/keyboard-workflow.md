# Keyboard workflow QA

The worksheet editor should be usable without a pointer.

Check that focus moves through controls in a predictable order, dialogs trap focus while open, Escape closes dismissible overlays, and visible focus indicators remain clear against the application background.
