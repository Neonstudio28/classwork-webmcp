# Keyboard accessibility QA

Every primary workflow should be reachable without a pointer. Check tab order, visible focus, Enter/Space activation, escape/cancel behavior, and focus restoration after modal or generated-content states.