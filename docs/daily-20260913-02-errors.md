# Stable error contract

User-facing failures should distinguish validation, unavailable providers, timeouts, and unexpected internal errors. Error messages should be actionable without exposing credentials, stack traces, or provider internals.