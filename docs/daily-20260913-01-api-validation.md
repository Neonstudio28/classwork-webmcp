# API validation baseline

Validate incoming tool arguments before invoking model or browser operations. Reject missing required fields, unexpected types, and oversized payloads early so downstream providers receive predictable inputs.