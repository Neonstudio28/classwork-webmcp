# Release preflight

Before deployment, run the production build and lint checks, verify required environment configuration, exercise the primary generation flow, and test provider failure handling. Keep rollback instructions adjacent to each release.